import { HSL, RGB } from "../types.js";
export declare const rgbToHsl: (r: number, g: number, b: number, a?: number) => HSL;
export declare const hslToRgb: (h: number, s: number, l: number, a?: number) => RGB;
